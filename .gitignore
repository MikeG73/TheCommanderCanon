# ✅ TheCommanderCanon: Deploy Mirror

# Nothing ignored — all files in this repo are live, deployable truth.
# DO NOT exclude .json, .lock, or manifest files.
# Prevent deploy confusion
public/asset-manifest.json
